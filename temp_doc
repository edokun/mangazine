
Posible options:
download <mangaId>
if no chapter parameter provided will download first chatper only
-c --chapters <chapter>
    (chapter list divided by , or range using [n-n], or single number
    example: -c 1   -c 1,3   -c [1-10]
-d --download-directory <location>
manga will be downloaded with structure manga_name/chapter_number/file_name(3 digits .jpg file),
if not provided will download on user's home directory

info <mangaId> will get general information of the manga
-


Global optional parameters:
-p --provider <provider> will specify source to download manga from, if not specified
will use any
